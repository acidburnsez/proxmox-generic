apiVersion: v1
kind: Service
metadata:
  name: longhorn-frontend-external
  namespace: longhorn-system
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 8083
    targetPort: http
    protocol: TCP
  selector:
    app: longhorn-ui
