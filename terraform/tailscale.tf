# ── Tailscale Subnet Router Decommissioned ──────────────────────────────────────
#
# The Tailscale subnet router has been successfully migrated to the K3s cluster
# as a native Deployment inside the `tailscale` namespace, with state persisted
# on Longhorn.
#
# The old LXC container (CT 500) resources have been removed from this file.
